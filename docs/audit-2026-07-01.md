# Codebase Audit — 2026-07-01 (Claude Code)

Full audit of every backend/lib/frontend/config file, verified live against the dev
instance and ESPN. This file tracks the consolidated fix plan; tick items as they land.
Full finding details live in the audit conversation; the short forms below are enough
to re-derive each fix.

## Critical findings (C) / Moderate (M) — reference list

- **C1** No `trust proxy` → all users shared one rate-limit bucket (confirmed ERR_ERL_UNEXPECTED_X_FORWARDED_FOR in journal)
- **C2** `/api/admin/*` + `/admin/` unauthenticated
- **C3** Stored XSS: feedback `page` unescaped in admin portal, unvalidated at POST
- **C4** Soccer live scoreboard slugs 400 — correct: `soccer/usa.ncaa.w.1` (W), `soccer/usa.ncaa.m.1` (M). NOTE: softball's `baseball/college-softball` team-schedule path is CORRECT (verified returns ASU softball) — do not "fix".
- **C5** SW cache-first freezes unlisted API GETs forever (`/api/seasons`, `/api/releases`, `/api/admin/*`, `/api/cf-stats`, …); feedback.js/css unversioned
- **C6** Unguarded `Notification` identifier throws on iOS Safari tabs: live.js:90, pwa.js:371,478 (use `typeof Notification === 'undefined'` like live.js `_buildNotifyBar`)
- **C7** Push lifecycle dead-end: 30-day purge + client skip-if-same-endpoint → permanent 409s; `_postSportPrefs` can insert empty-key rows; `sport_prefs` never read server-side (dead feature)
- **C8** `/api/subscribe` accepted arbitrary endpoints (blind SSRF)
- **M1** `findDBMatch` single-candidate day match skips opponent check; doubleheaders insert `espn_` dupes
- **M2** `/api/live` = ~9 sequential uncached ESPN fetches; tournaments add uncached per-game summaries (hot 30s path)
- **M3** Push payload times: server-TZ formatted string → **changed per Robert: send raw `startTime` epoch in payload; sw.js push handler formats in device-local TZ** (he travels; wants local time, not Phoenix). Also reuse `opponentFromTitle`, fix "15 min" copy.
- **M4** Cron `8-23` America/Chicago misses late Phoenix finishes; stale scheduler comment
- **M5** Geocoder: city/state-only rows re-selected forever, never geocoded; concurrent runs possible (nightly + /api/refresh + startup)
- **M6** Fetcher: no stale-event handling; `String(raw.id)` → `"undefined"` row collapse
- **M7** SQLite: `foreign_keys` off (CASCADE inert), no WAL/busy_timeout despite documented second instance on :3100
- **M8** filters.js/calendar.js inject feed/ESPN strings into innerHTML unescaped (live.js/game-modal.js are the good pattern)
- **M9** TtlCache unbounded; `espnGameCache` keyed by client input (id now validated numeric — cap still todo)
- **M10** Inconsistent error bodies / err.message leaks (fixed in Group 1)
- **M11** `loadFilterOptions` no error handling; fetches all `/api/events` just for default season
- **M12** DC naming mismatch (fetcher `Washington D.C.` vs REGIONS `District of Columbia`); REGIONS duplicated db.js/filters.js
- **M13** ncaa.js hardcodes `brackets/baseball/d1/2026`
- **M14** Countdown timer self-cancels when upcoming+final games coexist
- Minor: .gitignore junk lines, repo service file drift, dead `#status-bar`, hardcoded `seasonLabel`, order-dependent `sportColor`, unbounded `_notifiedIds`, whats-new raw localStorage, SW notification `tag` collision + offline fallback shape, modal focus traps, `detectActiveTournaments` live-as-final, `lib/env.js` key regex misses digits, CLAUDE.md structure list stale

## Fix plan / status

### Group 1 — Security & config — ✅ DONE 2026-07-01 (dev, verified)
- [x] `trust proxy` via `TRUST_PROXY_HOPS` (code default 1 = prod; dev drop-in sets 2) (C1)
- [x] `requireAdmin` (X-Admin-Token vs `ADMIN_TOKEN` in secrets.env; 503 fail-closed when unset) on all `/api/admin/*`; admin routes moved adminLimit→generalLimit; admin page prompts once, stores token, handles 401/503 (C2)
- [x] Feedback `page` whitelisted server-side + escaped in admin portal; UA capped 400 chars (C3)
- [x] `/api/subscribe*`: https+length endpoint validation; new subs require keys (C8 + part of C7)
- [x] `/api/game/:id` numeric validation (part of M9)
- [x] Generic error bodies everywhere; `from`/`to` NaN clamp in db.js queryEvents (M10)
- [x] sw.js: `/api/admin` → NETWORK_ONLY, CACHE_NAME v13→v14
- ⚠ **Prod deploy note**: generate a *separate* ADMIN_TOKEN into Oracle's `~/projects/secrets.env` before/with the next tag deploy. No TRUST_PROXY_HOPS needed on prod (default 1 correct for on-box cloudflared).

### Group 2 — Live-path correctness & performance — ✅ DONE 2026-07-01 (dev, verified)
- [x] Soccer fixed (C4) — bigger than slugs: verified `college-soccer-*` 400s for scoreboard AND summary; correct league codes `usa.ncaa.w.1`/`usa.ncaa.m.1`. Also the LIVE_EXTRA_SPORTS dbSport keys ("Women's/Men's Soccer") matched ZERO feed rows — feed tags soccer as bare `'Soccer'`; entry now `{ dbSport: 'Soccer', espnPath: 'soccer/usa.ncaa.w.1' }`, men's entry removed (ASU has no program). ESPN_SPORT_SLUGS 'Soccer'→w.1; RANKINGS_SLUGS gained 'Soccer'. Softball `baseball/college-softball` quirk documented in-code.
- [x] `fetchLiveGames`: 15s TtlCache + single-flight + `Promise.allSettled` parallel sweep (M2). Measured: cold 1.2s→0.28s, cached 5ms, concurrent misses share one sweep. tournaments.js: per-game summaries parallel + 60s cache; pool standings 60s cache. Still worth watching a real live-game day on dev before promoting.
- [x] `findDBMatch` (M1): espn_{id} exact match first (callers now attach espnEventId after extractScore); opponent required on lone same-day candidate (unless title unparseable — legacy shapes); doubleheaders → nearest start time, ambiguous placeholder times → null (auto-insert). 7-case synthetic test passed; findDBMatch exported for tests.

### Group 3 — Service worker & frontend caching — ✅ DONE 2026-07-01 (dev; needs real-device pass)
- [x] sw.js: all same-origin `/api/` GETs default to network-first (NETWORK_FIRST_PATHS list removed); NETWORK_ONLY grew `/admin/` + `/stats` (unversioned online-only HTML — a stale cached admin page would predate the token logic). Verified via a stubbed-SW behavioral test: 24-case routing table all correct (C5)
- [x] Offline fallback shape: array endpoints (`/api/events|sports|locations|seasons`) → `[]`; others → JSON 503 `{offline:true}` (C5)
- [x] feedback.js/feedback.css now versioned (`?v=1`); live.js→v30, pwa.js→v6; CACHE_NAME stays v14 (Group 1 bump, unshipped — one activation covers the release)
- [x] `typeof Notification` guards: live.js `_checkAndNotify`, pwa.js debug panel / `_initPushSubscription` / `_renderNotifSection` (skips section entirely when unavailable) / DOMContentLoaded init (C6). Grep-audited: remaining bare refs all behind guards.
- [x] Headless-Chromium smoke render OK (Live tab, countdown, standings, feedback widget)
- [ ] **Still open**: real-device pass — iPhone Safari tab (Live tab side-effects now survive polling) + installed PWA v13→v14 SW update/reload flow

### Group 4 — Push lifecycle — ✅ DONE 2026-07-02 (dev; needs real-device push test)
- [x] C7: 30-day orphan purge REMOVED from `cleanupExpiredSubscriptions` (410s at send time are the only push_subscriptions cleanup); client now always re-POSTs `/api/subscribe` on load (skip-if-known-endpoint removed — that was the stranding mechanism); `_applyBellTypes` recovers from 409 by re-registering + one retry. sport_prefs decision: **UI removed** (checkboxes never drove anything server-side; wiring broadcast = spam risk — noted as future feature instead). `asu-sport-prefs` localStorage + sport_prefs column left in place, harmless.
- [x] M3 (local-time): buildPayload sends raw `startTime` epoch, no server-TZ string; sw.js push handler renders "Starts in N min (H:MM)" in device-local TZ at display time; old/new payload shapes both graceful (5-case stubbed-SW test passed). Uses `opponentFromTitle`; "in 15 min" copy gone.
- [x] Notification tags: game start `asu-start-{id}`; score updates + final share `asu-game-{id}` (final replaces last score update; old shape falls back to constant tag).
- [x] M4: cron windows `0-1,8-23` (covers post-midnight-CT Phoenix finishes; validated with node-cron); stale scheduler comments rewritten.
- [x] pwa.js→v7, CACHE_NAME→v15. Service restarted, smoke + render clean.
- [x] Side discovery: dev secrets were consolidated — `unifi-scripts/secrets.env` no longer exists; VAPID_* now in `~/projects/secrets.env` (CLAUDE.md updated). `lib/env.js` fallback is dead code on both boxes → remove in Group 6.
- [ ] **Still open**: real-device push test (test-push.js start/final against a phone-subscribed game; shell runs must source `~/projects/secrets.env` for VAPID)

### Group 5 — Data integrity — ✅ DONE 2026-07-02 (dev, verified)
- [x] M7: WAL + busy_timeout=5000 + foreign_keys=ON in db.js (journal_mode verified 'wal'; two-instance concurrent read/write test clean; FK cascade + FK enforcement unit-tested PASS). `/api/subscribe/game` add-path now 400s unknown eventIds (removals still allowed for pruned events). `.gitignore` gained `events.db-*` (+ junk lines removed). **Discovery: the documented PORT=3100 knob collides with hotel-search on the dev VM — docs now say :3200.**
- [x] M6: fetcher id guard (`raw.id != null`), and `deleteStaleFutureFeedEvents` prunes canceled games with three guards: espn_* excluded via NOT GLOB (not LIKE — underscore wildcard), feed-horizon bound, >20-stale hiccup bailout. 3 live-DB tests PASS (targeted delete, hiccup guard, horizon guard).
- [x] M5: geocoder serialized (in-flight guard verified via concurrent /api/geocode) + city/state fallback for address-less events — required also adding city/state to getEventsNeedingGeocode's SELECT (they were never fetched). The 3 stuck events (Lincoln, Nebraska) geocoded on the spot; backlog now 0. Remaining 4 perpetual failures are KNOWN_VENUES candidates: 68-1050 Makaiwa Pl Waimea HI / Carretera Transpeninsular (MX) / 3700 SW Floridian Dr Palm City FL / 2400 Brunes Mill Rd Columbus TX → Group 6.
- [x] M12: DC → 'District of Columbia' in fetcher STATE_MAP (0 existing rows to migrate); REGIONS deduped — served from db.js via new `GET /api/regions`, filters.js copy deleted (shape-validated against the SW offline fallback), filters.js→v22.
- [x] M13: ncaa.js year derived (current year, fallback to previous) + sport param on getNcaaConfig (cache key per sport). Live-verified: `baseball/2026` config loads.

### Group 6 — Frontend polish & cleanups — ✅ DONE 2026-07-02 (dev, verified)
- [x] M8 `esc()` sweep: filters.js (logo attrs w/ JS-string+HTML double-escape, all modal rows, ticket/node links now http(s)-only) + calendar.js (eventContent, listEventHTML, ticket link scheme check). **CSP: deliberately skipped** — the UI is built on inline onclick handlers throughout, so script-src would need 'unsafe-inline', making the policy near-worthless; revisit only if handlers ever get refactored to addEventListener.
- [x] M14 countdown element-existence guards (shared timer can no longer self-cancel). M11: loadFilterOptions wrapped (sidebar shows retry link + toast on failure) and the full `/api/events` fetch replaced by new `GET /api/seasons/default` (db.getDefaultSeason — same non-compound season set as the dropdown).
- [x] Minors: `#status-bar` removed (index.html + style.css→v11); seasonLabel computed from the value (7-case test PASS); sportColor hashed = stable across sessions (test PASS); `_notifiedIds` → timestamped map pruned at 48h (migrates old array); whats-new uses `store`; TtlCache FIFO-capped at 500 (test PASS); modal a11y (role/aria-modal + focus-in/restore on event modal & game modal — full tab-trap like feedback.js left undone); tournaments.js stable matchup ids + DB-fallback state no longer shows in-progress games as FINAL (5h live window); repo asu-cal.service synced with per-box template comments (EnvironmentFile no longer `-`-prefixed — missing secrets should fail loudly); CLAUDE.md structure block refreshed.
- [x] `lib/env.js` DELETED (dead on both boxes); server.js/test-push.js/push.js references cleaned; test-push warns to source secrets.env.
- [x] KNOWN_VENUES: 4 golf venues pinned (Mauna Lani, Twin Dolphin Cabo, Floridian, Big Easy Ranch) — **geocode backlog is now 0, every event has coordinates**.
- [x] Versions: shared.js→5, filters.js→23, calendar.js→15, live.js→31, whats-new.js→4, game-modal.js→5, style.css→11, CACHE_NAME→v16. Full syntax+unit checks, restart, endpoint smoke, headless render all clean.

### Addendum — Mobile fixes (Robert-reported, 2026-07-02) — ✅ DONE, verified
- [x] **Filter sheet**: the mobile sidebar was `order:2` below a 100vh main — opening it landed off-screen at the page bottom, unreachable (repro'd: sidebar top=816 in an 844px viewport). Now a fixed bottom sheet: 75dvh max (vh fallback), safe-area-inset-bottom padding, grabber bar, dimmed backdrop (tap-anywhere dismisses, incl. over the header — standard sheet behavior), Escape closes, switching to Live closes programmatically (bfcache path), aria-expanded/aria-controls on the toggle.
- [x] **Header width**: non-Live tabs overflowed 72px at 390px (Filters btn only shows off-Live; theme+refresh were off-screen, page scrolled sideways). Fix: ≤767px hides the wordmark (Sparky only) + the header 📅 (moved into the sheet as "Subscribe in your calendar app"), tighter gaps, clamp()-fluid view-toggle sizing, `overflow-x: clip` safety net on header.
- [x] Verified via Playwright sweep: 320/360/390/430px × all 4 tabs → zero page overflow (at 320 the clip guard absorbs ~12px); sheet within viewport + interactions (sport tap, backdrop dismiss, Escape, setView-live) PASS; desktop 1280px regression PASS (inline sidebar, no backdrop, header 📅 back, wordmark back).
- style.css→v12, filters.js→v24, CACHE_NAME→v17. Real-device confirmation on Robert's phone still recommended (rubber-band scrolling, home-indicator).

---
## ALL SIX GROUPS COMPLETE + mobile addendum (2026-07-02) — uncommitted on dev
Remaining before prod promotion:
1. Commit + version bump (package.json 1.2.1 → 1.3.0) + releases.json entry + tag
2. Real-device pass: iPhone Safari tab live-poll behavior, installed-PWA v13→v16 SW update flow, push test (test-push.js start/final with sourced secrets)
3. Prod deploy extras: generate prod ADMIN_TOKEN into Oracle secrets.env; NO TRUST_PROXY_HOPS on prod; verify VAPID_* present there
4. Watch one real live-game day on dev (live-cache behavior) before or shortly after promoting

## Feature ideas (approved-for-later pool, not scheduled)
1. Filtered iCal subscribe (filters.js only)
2. Top-25 poll widget (`/api/rankings` + standings widget tab; data already cached server-side)
3. Next-game-per-favorite-sport strip on Live tab (keep out of ESPN fetch path)
4. Per-game .ics with VALARM (lib/ical.js + route + modal button)
5. `/api/summary` for scheibdash tile (serve from the Group 2 live cache)
